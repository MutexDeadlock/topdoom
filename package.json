{
  "name": "topdoom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "test": "node --test --disable-warning=ExperimentalWarning \"tests/**/*.test.ts\"",
    "test:watch": "node --test --watch --disable-warning=ExperimentalWarning \"tests/**/*.test.ts\"",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "relay": "npm --prefix server start"
  },
  "keywords": [],
  "type": "module",
  "devDependencies": {
    "@types/node": "^26.5.1",
    "@types/three": "^0.185.4",
    "playwright-core": "^1.63.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
